FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package.json ./

# Use npm install instead of npm ci to handle version mismatches
RUN npm install

# Copy the rest of the app
COPY . .

EXPOSE 3001

CMD ["npm", "run", "dev"]
